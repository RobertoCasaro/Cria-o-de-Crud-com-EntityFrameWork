@model TesteVeranine.Models.FichaMed

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>FichaMed</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Idmed" class="control-label"></label>
                <select asp-for="Idmed" class ="form-control" asp-items="ViewBag.Idmed"></select>
            </div>
            <div class="form-group">
                <label asp-for="Idpac" class="control-label"></label>
                <select asp-for="Idpac" class ="form-control" asp-items="ViewBag.Idpac"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
